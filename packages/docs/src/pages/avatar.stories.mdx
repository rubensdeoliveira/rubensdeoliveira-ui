import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { Avatar } from '@rubensdeoliveira-ui/react'

<Meta 
  title="Avatar" 
  argTypes={{
    name: {
      name: 'Badge Content',
      description: 'Text shown by Badge',
      control: {
        type: 'text'
      },
      table: {
        type: {
          summary: 'The label contents',
          detail: 'Text displayed by the Badge'
        }
      }
    },
  }} 
  component={Avatar} />

# Avatar

This is avatar

<ArgsTable of={Avatar} />

<Canvas>
  <Story name="Avatar" args={{ name: 'ds' }}>
    {(args) => <Avatar {...args} />}
  </Story>
</Canvas>

