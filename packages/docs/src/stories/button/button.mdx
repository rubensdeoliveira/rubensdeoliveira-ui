import { Meta, Primary, Controls, Story, Canvas } from '@storybook/blocks';
import * as ButtonStories from './button.stories';

<Meta of={ButtonStories} />

# Button

The Button component is intended to render different types of button.

<Primary />

## Examples

### Filled

Rendered button with background filled.

<Canvas>
  <Story of={ButtonStories.Filled} />
</Canvas>

### Bordered

Rendered button with border and no background.

> When name is bigger than two words rendered first and last names initials, and when name has only one name rendered name initial.

<Canvas>
  <Story of={ButtonStories.Bordered} />
</Canvas>

### Ghosted

Rendered button with only content without border or background.

<Canvas>
  <Story of={ButtonStories.Ghosted} />
</Canvas>

## Propriedades

<Controls />